spring:
  datasource:
    url: jdbc:postgresql://${PG_DB_HOST:postgresdb}:${PG_DB_PORT:5432}/${POSTGRES_DB_CATEGORY_SERVICE:category_service_db}
    username: ${PG_DB_USERNAME_CATEGORY_SERVICE:category_service}
    password: ${PG_DB_PASS_CATEGORY_SERVICE:category_service}