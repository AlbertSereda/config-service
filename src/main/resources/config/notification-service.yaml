spring:
  datasource:
    url: jdbc:postgresql://${PG_DB_HOST:postgresdb}:${PG_DB_PORT:5432}/${POSTGRES_DB_NOTIFICATION_SERVICE:notification_service_db}
    username: ${PG_DB_USERNAME_NOTIFICATION_SERVICE:notification_service}
    password: ${PG_DB_PASS_NOTIFICATION_SERVICE:notification_service}