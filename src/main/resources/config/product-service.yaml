spring:
  datasource:
    url: jdbc:postgresql://${PG_DB_HOST:postgresdb}:${PG_DB_PORT:5432}/${POSTGRES_DB_PRODUCT_SERVICE:product_service_db}
    username: ${PG_DB_USERNAME_PRODUCT_SERVICE:product_service}
    password: ${PG_DB_PASS_PRODUCT_SERVICE:product_service}