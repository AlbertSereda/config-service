spring:
  datasource:
    url: jdbc:postgresql://${PG_DB_HOST:postgresdb}:${PG_DB_PORT:5432}/${POSTGRES_DB_RATING_SERVICE:rating_service_db}
    username: ${PG_DB_USERNAME_RATING_SERVICE:rating_service}
    password: ${PG_DB_PASS_RATING_SERVICE:rating_service}